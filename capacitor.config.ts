import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.rireader.app',
  appName: 'ri-reader',
  webDir: 'dist/ri-reader',
  bundledWebRuntime: false
};

export default config;
